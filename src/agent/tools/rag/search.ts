// import { createVectorStore } from '@/utils/vectors/index'

// export const searchRAG = async ({ query }: { query: string }) => {
//   const store = await createVectorStore()
//   const results = await store.similaritySearch(query, 4)
//   return JSON.stringify(results.map((r) => r.pageContent))
// }
