{
  "name": "agent-administratif-hono",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "clean": "rm -rf dist && echo 'Done.'",
    "auth:generate": "bunx auth@latest generate",
    "db:migrate": "bunxdrizzle-kit migrate",
    "db:push": "bunx drizzle-kit push",
    "db:studio": "bunx drizzle-kit studio"
  },
  "dependencies": {
    "better-auth": "^1.6.14",
    "drizzle-orm": "^0.45.2",
    "hono": "^4.12.23",
    "pg": "^8.21.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^0.31.10"
  },
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "peerDependencies": {
    "typescript": "^5"
  }
}