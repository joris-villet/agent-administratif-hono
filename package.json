{
  "name": "agent-administratif-hono",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "dev:telegram": "bun run scripts/set-webhook-telegram.ts",
    "dev:vault": "bun run scripts/dev/vaultwarden.ts",
    "clean": "rm -rf dist && echo 'Done.'",
    "auth:generate": "bunx auth@latest generate",
    "db:generate": "bunx drizzle-kit generate",
    "db:check": "bunx drizzle-kit check",
    "db:migrate": "bunx drizzle-kit migrate",
    "db:push": "bunx drizzle-kit push",
    "db:studio": "bunx drizzle-kit studio"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.8.0",
    "@langchain/langgraph-checkpoint-postgres": "^1.0.3",
    "@langchain/openai": "^1.4.7",
    "@spawn/docker": "npm:@jsr/spawn__docker",
    "better-auth": "^1.6.14",
    "drizzle-orm": "^0.45.2",
    "hono": "^4.12.23",
    "langchain": "^1.4.4",
    "pg": "^8.21.0",
    "shelljs": "^0.10.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^0.31.10"
  },
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "peerDependencies": {
    "typescript": "^7.0.1-rc"
  }
}
